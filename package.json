{
  "name": "scanandstock-showcasev2",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev --port 4321",
    "sync:notion": "node --env-file=.env scripts/sync-notion.mjs",
    "prebuild": "node --env-file=.env scripts/sync-notion.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "test": "node --test scripts/"
  },
  "dependencies": {
    "@notionhq/client": "^2.3.0",
    "astro": "^6.3.1",
    "notion-to-md": "^3.1.9",
    "sharp": "^0.34.5"
  }
}